# Finance Bot — UX Specification v0.1

## 1. Общая концепция
Финансовый Telegram-бот для учёта личных и семейных финансов, с поддержкой нескольких бюджетов и совместного ведения (семья/друзья).

**Цели:**
- быстрый и удобный ввод доходов и расходов;
- надёжное хранение данных;
- понятные отчёты и сводки;
- постепенное расширение аналитики и AI-функций.

**Принципы UX:**
- минимум визуального шума;
- контроль пользователя над каждым действием;
- отсутствие «магии» в деньгах;
- бот — инструмент, а не бухгалтер.

**Сознательные ограничения:**
- бот не принимает финансовых решений за пользователя;
- AI никогда не создаёт операции автоматически;
- сложная аналитика и графики — вне MVP.

---

## 2. Пользователи, бюджеты и совместный доступ

### 2.1 Регистрация
- Регистрация не требует логина/пароля: пользователь идентифицируется Telegram-аккаунтом.
- При первом запуске пользователь создаёт **первый бюджет** или **присоединяется** к существующему.

#### Создание первого бюджета (MVP)
1. Бот спрашивает название бюджета.
2. Бот запрашивает базовую валюту (код ISO из 3 букв).
3. Бот предлагает указать до двух вспомогательных валют или пропустить шаг.
4. Бот запрашивает таймзону бюджета (IANA, с подсказкой значения по умолчанию).
5. Бот показывает подтверждение и создаёт бюджет после согласия пользователя.
6. На каждом шаге доступна **Отмена**.

### 2.2 Бюджеты (пространства)
- Пользователь может иметь несколько бюджетов (например: «Личный», «Семья», «Поездка»).
- У бюджета всегда есть **активный бюджет по умолчанию**: ввод операций и отчёты выполняются в рамках активного бюджета.
- Переключение активного бюджета — **только через Настройки** (чтобы не нагружать основной UX).

### 2.3 Участники и приглашения
- В бюджете могут быть участники.
- Добавление участника — через **пригласительную ссылку** (инвайт), которую создаёт владелец бюджета.
- При переходе по ссылке бот показывает название бюджета и просит подтверждение присоединения.

### 2.4 Роли (MVP)
- **Владелец**: управляет участниками, настройками бюджета, валютами бюджета.
- **Участник**: вводит операции и смотрит отчёты в рамках бюджета.

---

## 3. Валюты бюджета

### 3.1 Валютный контур бюджета
При создании бюджета выбираются:
- **основная валюта** бюджета;
- **две дополнительные валюты** (опционально).

Пример:
- основная: RSD; дополнительные: EUR, USD
- основная: RUB; дополнительные: USD, EUR

### 3.2 Выбор валюты при вводе операций
- При добавлении операций (приход/расход/копилка) выбор валюты ограничен валютами бюджета: **основная + дополнительные**.
- Валюта показывается как inline-выбор с дефолтом (обычно — основная валюта бюджета).

---

## 4. Основные сценарии

### 4.1 Добавление прихода
1. Пользователь нажимает кнопку **«Приход»** (reply-клавиатура).
2. Бот спрашивает сумму.
3. Бот предлагает выбрать валюту (inline, с дефолтом, только из валют бюджета).
4. Бот предлагает выбрать источник дохода (top-8 + «Ещё»).
5. Бот показывает подитог и просит подтверждение.
6. На всех шагах доступна кнопка **«Отмена»**.

### 4.2 Добавление расхода
Аналогично сценарию прихода, с выбором **категории расхода**.

---

## 5. Голосовой ввод
1. Пользователь отправляет голосовое сообщение.
2. Бот распознаёт речь (STT).
3. Бот пытается классифицировать сумму, валюту и категорию.
4. Бот **никогда** не создаёт операцию автоматически.
5. Пользователю показывается подитог с возможностью:
   - сохранить;
   - изменить;
   - отменить.

Если бот не уверен — он задаёт уточняющие вопросы.

---

## 6. Фото чека / скриншота
1. Пользователь отправляет фото.
2. Бот распознаёт текст (OCR).
3. Бот пытается определить сумму, валюту, категорию.
4. Всегда показывается подитог и требуется подтверждение.

---

## 7. Подтверждение, отмена и исправления

### 7.1 Подтверждение перед сохранением
Перед созданием любой операции пользователь видит сводку данных и подтверждает действие.

### 7.2 Отмена операции
- На каждом шаге сценария есть кнопка **«Отмена»**.
- После сохранения операции доступна кнопка **«Отменить»**.

### 7.3 Исправление операции
- После сохранения операции доступна кнопка **«Исправить»**.
- Исправление реализуется как отмена старой записи и создание новой.

### 7.4 Удаление через меню
В синем меню доступна команда **«Удалить запись»**:
- ввод даты;
- выбор операции из списка (inline);
- подтверждение удаления.

Все операции имеют **сквозную нумерацию**, которая не меняется.

---

## 8. Повтор операций
После успешного сохранения бот предлагает:
- **«Ещё приход / ещё расход»**;
- **«Повторить с теми же параметрами»**;
- **«Готово»**.

Это снижает количество действий при серии операций.

---

## 9. Копилка и цели

### 9.1 Создание цели
- указание названия;
- целевой суммы;
- валюты (из валют бюджета);
- **типа копилки (области видимости):**
  - **🔒 Личная** — доступна только создателю;
  - **👥 Общая** — доступна всем участникам бюджета.

### 9.2 Пополнение и изъятие
1. Кнопка **«Копилка»**.
2. Выбор действия: внести / взять.
3. Ввод суммы.
4. Выбор цели (с учётом доступности: личные цели видны только владельцу, общие — всем участникам бюджета).
5. Подтверждение.

Копилка — отдельный сценарий, не обычный расход.

---

## 10. Отчёты

### 10.1 Сводка
Краткая сводка за период:
- доходы;
- расходы;
- баланс;
- топ-3 категорий расходов.

### 10.2 Операции
- выбор периода: сегодня / неделя / месяц / произвольный;
- фильтр по типу: все / приход / расход / копилка;
- список операций с итогами.
- при больших списках — пагинация (показать ещё).

### 10.3 Цели
- активные цели;
- завершённые цели;
- отображение прогресса накоплений;
- для общей копилки — отображение доступно всем участникам бюджета.

---

## 11. Настройки

### 11.1 Бюджеты
- список бюджетов пользователя;
- переключение активного бюджета;
- управление участниками (для владельца): список участников, создание инвайт-ссылки.

### 11.2 Валюты бюджета
- выбор основной валюты и доп. валют (до 2) при создании бюджета;
- в MVP изменение валют — только владельцем (опционально; по умолчанию можно запретить изменение).

### 11.3 Категории и источники
- добавление категорий расходов и источников дохода;
- переименование;
- архивирование;
- сортировка категорий автоматически по частоте использования;
- показ top-8 (2 колонки) + «Ещё».

---

## 12. Сообщения и стиль

**Принципы:**
- лаконичность;
- 1–2 эмодзи максимум;
- пустые строки между смысловыми блоками;
- отсутствие канцелярита и CAPS.

### Пример: операция сохранена
```
✅ Расход сохранён

💸 −450 RSD
Категория: Продукты
№124
```

---

## 13. Роль AI

AI используется для:
- распознавания голоса;
- распознавания текста на изображениях;
- классификации предложений.

AI **не используется** для:
- расчётов;
- автоматического создания операций;
- принятия финансовых решений.

---

## 14. Вне рамок MVP
- графики и визуальные дашборды (mini-app);
- бюджеты по категориям;
- автоматические советы и алерты;
- сложные фильтры и прогнозы;
- расширенная ролевая модель (read-only и т.п.).
